{
  "name": "ejs-site",
  "version": "1.0.0",
  "description": "Marketing site and documentation for EJS (Embedded JavaScript templates)",
  "private": true,
  "dependencies": {},
  "scripts": {
    "docs:dev": "npm --prefix docs-src run dev",
    "docs:build": "npm --prefix docs-src run build",
    "docs:preview": "npm --prefix docs-src run preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mde@fleegix.org",
  "license": "ISC"
}
